# FurnaceRecipes.cpp examples

Only add these if you want smelting paths.

```cpp
// Example only - usually not used for these exact items in vanilla style
// addFurnaceRecipy(Item::beetroot_Id, new ItemInstance(Item::something), 0.1f);
```

## Notes
For the current item set in this patch, you likely do not need furnace recipes first.
Test registration and rendering before touching smelting.
